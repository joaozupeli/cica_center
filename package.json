{
  "name": "cica-center",
  "version": "1.0.0",
  "description": "Cica Center - Som, Rodas, Pneus e Acessórios",
  "main": "index.js",
  "scripts": {
    "build": "cd client && pnpm install && pnpm run build",
    "build-client": "cd client && pnpm install && pnpm run build",
    "build-server": "cd server && pnpm install && pnpm run build",
    "dev": "cd client && pnpm run dev",
    "lint": "cd client && pnpm run lint",
    "format": "cd client && pnpm run format"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
